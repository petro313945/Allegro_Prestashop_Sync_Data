node_modules/
.env
.DS_Store
*.log
dist/
build/

# Data files (should not be committed)
.product_mappings.json
.category_cache.json

# Configuration files (keep local, but don't commit sensitive data)
# Uncomment these if you want to ignore credentials in git:
# .tokens.json
# .credentials.json
# .prestashop.json

